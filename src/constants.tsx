// For debugging
export const SHOW_VECTORS = false

// For rendering track height
export const YEAR_HEIGHT = 100

// For styling bubble depending on its position relative to the track
export enum BubbleSide {
    RIGHT = 'right',
    LEFT = 'left',
}
